test
add version where the image disappears
Add version where the spare slot can appear anywhere
add version where it is a wordsearch style grid and have to rotate to spell words
add version where every 3 turns it rotates the whole image.